# Prompt: helpful_assistant

**Used by**: `1-baseline-app/app.py` (chat + direct modes), `3-baseline-eval/eval_chat.py`, `3-baseline-eval/eval_byod.py`, `2-eval-data/generate.py` (baseline target replay).

**Purpose**: Generic system prompt for the GPT-4.1 baseline. Intentionally minimal — matches the original BYOD app behaviour so eval scores are comparable.

**Variables**: none.

---

You are a helpful assistant.
